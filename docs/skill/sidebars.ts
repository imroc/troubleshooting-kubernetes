import type { SidebarsConfig } from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  skillSidebar: [
    'README',
    'linux',
    'enter-netns-with-nsenter',
    'remote-capture-with-ksniff',
    'use-systemtap-to-locate-problems',
    'tcpdump',
    'wireshark',
  ],
};

export default sidebars;
